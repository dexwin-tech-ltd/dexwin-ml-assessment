# Optional Dev Container

A Cursor / VS Code terminal plus `make setup` is enough for this assessment. You do not need Docker.

If you prefer a container, this folder’s `devcontainer.json` uses the Python 3.12 image and installs `requirements.txt` after create.
